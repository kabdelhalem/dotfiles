# ── SwayOSD server config ──────────────────────────────
# Mac-style volume/brightness overlay, themed to match waybar/rofi

[server]
show_percentage = true
max_volume = 100
# 0.0 = top of screen, 1.0 = bottom; 0.5 = centered
top_margin = 0.5
style = "/home/kabdelhalem/.config/swayosd/style.css"
